/* Online C Compiler and Editor */
#include <stdio.h>


int main ()
{
    int x=1;
    int y = 2;
    char nome[40]="nome";
    if(x > y) printf();
    if(x < y) printf();
    if(x >= y) printf();
    if(x <= y) printf();
    if(x == y) printf();

    // else(x != y)
    else printf();
    printf("%s", nome)

    return 0;
}
